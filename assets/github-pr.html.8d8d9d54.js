import{_ as e}from"./pr-fork.7b43a8a3.js";import{_ as o,o as t,c as a,e as s}from"./app.8e3baf58.js";const n={},i=s('<h1 id="github-pr" tabindex="-1"><a class="header-anchor" href="#github-pr" aria-hidden="true">#</a> Github-Pr</h1><h2 id="fork-the-project-you-want-to-participate-in" tabindex="-1"><a class="header-anchor" href="#fork-the-project-you-want-to-participate-in" aria-hidden="true">#</a> fork the project you want to participate in</h2><p>There are many excellent open source projects on github. Many times we are not only satisfied with using, but also want to participate in the development of open source projects and contribute our own code. Today&#39;s article will show you how to submit PR on github, let&#39;s contribute to the open source community together.</p><p>Find the open source project you want to participate in on github (here, ant-design is used as an example), and then click the fork button in the upper right corner, a copy of this open source project will be created in your own code repository, and all subsequent code submissions will be made commit to this repo so you don&#39;t have to worry about unreviewed code messing up the original repo.</p><p><img src="'+e+`" alt="pr-fork"></p><p>After completing the fork step, a copy of the open source project you want to participate in will be created in your own code repository.</p><h2 id="clone-the-new-repository-to-local" tabindex="-1"><a class="header-anchor" href="#clone-the-new-repository-to-local" aria-hidden="true">#</a> Clone the new repository to local</h2><p>Execute the git clone command to clone this copy locally (ssh is recommended to avoid entering a user name and password).</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git clone git@github.com:astonishqft/ant-design.git</span></span>
<span class="line"></span></code></pre></div><h2 id="modify-the-code-and-submit" tabindex="-1"><a class="header-anchor" href="#modify-the-code-and-submit" aria-hidden="true">#</a> Modify the code and submit</h2><p>Create a local branch and make code changes:</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git checkout -b dev origin/master</span></span>
<span class="line"></span></code></pre></div><p>After modification, submit the changed code:</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git add xxx</span></span>
<span class="line"><span style="color:#D4D4D4;">git commit -m </span><span style="color:#CE9178;">&#39;xxx commit&#39;</span></span>
<span class="line"><span style="color:#D4D4D4;">git push -u orign dev:master</span></span>
<span class="line"></span></code></pre></div><p>At this point, the code has been successfully modified and submitted to your fork repository. In theory, PR can be submitted. However, you need to consider that in the process of modifying the code, the open source repository may also submit the code synchronously, so you need to do a code synchronization operation before submitting the PR.</p><p>If you are sure that the repository has not changed, you can skip the synchronization operation</p><p>Use the <code>git remote</code> command to establish a link between the original repository and the local repository.</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git remote add upstream git@github.com:ant-design/ant-design.git</span></span>
<span class="line"></span></code></pre></div><p>Execute the git fetch upstream command to get the updates of the remote host to the local.</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git fetch upstream</span></span>
<span class="line"></span></code></pre></div><p>Execute the <code>git rebase</code> command to merge the updates. The difference between <code>git rebase</code> and <code>git merge</code> is that <code>git rebase</code> forms a line. It is recommended that you use the <code>git rebase</code> command.</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git rebase upstream/master</span></span>
<span class="line"></span></code></pre></div><p>If a conflict is encountered, execute the following command after resolving the conflict.</p><div class="language-bash ext-sh"><pre class="shiki" style="background-color:#1E1E1E;"><code><span class="line"><span style="color:#D4D4D4;">git add conflicting files</span></span>
<span class="line"><span style="color:#D4D4D4;">git rebase --continue</span></span>
<span class="line"></span></code></pre></div><p>At this point, the preparations before PR are all completed, and the PR operation can be performed.</p><h2 id="submit-pr" tabindex="-1"><a class="header-anchor" href="#submit-pr" aria-hidden="true">#</a> Submit PR</h2><p>Open the open source project fork on github and create a pull request.</p><p>Carefully read the author&#39;s Pr detailed specification, and submit according to the specification for review</p>`,28),r=[i];function c(p,l){return t(),a("div",null,r)}var u=o(n,[["render",c],["__file","github-pr.html.vue"]]);export{u as default};
