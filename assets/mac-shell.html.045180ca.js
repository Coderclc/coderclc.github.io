import{_ as e,o,c as t,e as c}from"./app.8e3baf58.js";const l={},n=c('<h1 id="mac-shell" tabindex="-1"><a class="header-anchor" href="#mac-shell" aria-hidden="true">#</a> Mac Shell</h1><p>Install <code>pnpm</code> globally via <code>npm</code> when learning to use the Mac Shell</p><p>error</p><p><code>The operation was rejected by your operating system</code></p><p>View the npm global installation location <code>npm get config prefix</code> is the system disk under <code>/usr/local</code></p><p>A better solution is to modify the location of the npm global installation script</p><p><code>npm config set prefix &#39;~/.npm/_global&#39;</code></p><p>At this point, executing <code>npm install pnpm -g</code> will install it to the <code>&#39;~/.npm/_global&#39;</code> location, but still cannot find <code>pnpm</code> in the shell</p><p>Because it is not added to the shell PATH environment, there are two types of mac shells, <code>bash</code> and <code>zsh</code></p><p>The user-defined PATh script under <code>bash</code> is <code>.bash_profile</code> corresponding to <code>.zprofile</code> under <code>zsh</code></p><p>For example, <code>echo export PATH=~/.npm/_global/bin:$PATH &gt; ~/.bash_profile</code> under bash is updating the shell with source <code>source ~/.bash_profile</code></p><p>zsh is the same</p>',12),s=[n];function a(d,i){return o(),t("div",null,s)}var r=e(l,[["render",a],["__file","mac-shell.html.vue"]]);export{r as default};
